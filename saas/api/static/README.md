# `api/static` Directory

This directory contains static files that are served directly by the API server.

### Structure

- **`robots.txt`**: A standard file to instruct web crawlers on which pages or files the crawler can or can't request from your site. 